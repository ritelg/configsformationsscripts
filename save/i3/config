
set $left h
set $right l
set $top k
set $bottom j


set $mod Mod4
font pango:DejaVu Sans Mono 11
font pango:DejaVu Sans Mono 11

# Xss-lock et nm-applet
        # The combination of xss-lock, nm-applet and pactl is a popular choice, so
        # they are included here as an example. Modify as you see fit.

        # xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
        # screen before suspend. Use loginctl lock-session to lock your screen.
        exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

        # NetworkManager is the most popular way to manage wireless networks on Linux,
        # and nm-applet is a desktop environment-independent system tray GUI for it.
        exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
        set $refresh_i3status killall -SIGUSR1 i3status
        bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
        bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
        bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
        bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status


floating_modifier $mod
bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Shift+A kill

bindsym $mod+$left focus left
bindsym $mod+$bottom focus down
bindsym $mod+$top focus up
bindsym $mod+$right focus right

bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$bottom move down
bindsym $mod+Shift+$top move up
bindsym $mod+Shift+$right move right

bindsym $mod+v split v
bindsym $mod+f fullscreen toggle
bindsym $mod+z layout tabbed
bindsym $mod+e layout toggle split
bindsym $mod+Shift+f floating togglea
bindsym $mod+q focus parent
#bindsym $mod+v focus child

bindsym $mod+r mode "resize"

bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Voulez vous vraiment fermer la session' -B 'OUI, fermer i3' 'i3-msg exit'"


# Focus la fenêtre au survol
focus_follows_mouse yes

# Couleurs
set_from_resource       $active       active
set_from_resource       $inactive     secondary
set_from_resource       $background   background
set_from_resource       $foreground   foreground

# class			border	    backgr.   	text	       indicat   child_border
client.focused          $background #2A2D3D     $inactive     #FFCC66   $active
client.focused_inactive $background #2A2D3D 	$inactive    #5E6386   $inactive
client.unfocused        $background #222431     $inactive      #34384D   $inactive
client.urgent           $active     $active    	$background    #900000   $active
client.placeholder      $background $background $foreground    #000000   $inactive
client.background       $background

# vars workspaces 
set $ws1 1
set $ws2 2
set $ws3 3
set $ws4 4
set $ws5 5
set $ws6 6
set $ws7 7
set $ws8 8
set $ws9 9
set $ws10 0
set $moniteurprincipale LVDS-1
set $moniteurhdmi HDMI-1
set $moniteursecondaire VGA-1

# Ecran associé à chaque Workspace
workspace $ws1 output $moniteurhdmi
workspace $ws2 output $moniteurhdmi
workspace $ws3 output $moniteurhdmi
workspace $ws4 output $moniteurhdmi
workspace $ws5 output $moniteurhdmi
workspace $ws6 output $moniteursecondaire
workspace $ws7 output $moniteurprincipale
workspace $ws8 output $moniteurprincipale
workspace $ws9 output $moniteurprincipale
workspace $ws10 output $moniteurhdmi

# assignation programme
# Identifier programme : xprop | grep CLASS
assign [class="jetbrains-idea"] $ws10
assign [class="jetbrains-phpstorm"] $ws10
assign [class="Code"] $ws1
assign [class="URxvt"] $ws2
assign [class="Google-chrome"] $ws3
assign [class="discord"] $ws5

# Lancements des fenetres au demarrages
exec --no-startup-id compton
exec --no-startup-id i3-sensible-terminal

bindsym $mod+d exec rofi -show drun
bindsym $mod+o exec rofi -show combi
bindsym $mod+t exec rofi -show run

# switch to workspace
bindsym $mod+ampersand workspace number $ws1
bindsym $mod+eacute workspace number $ws2
bindsym $mod+quotedbl workspace number $ws3
bindsym $mod+apostrophe workspace number $ws4
bindsym $mod+parenleft workspace number $ws5
bindsym $mod+minus workspace number $ws6
bindsym $mod+egrave workspace number $ws7
bindsym $mod+underscore workspace number $ws8
bindsym $mod+ccedilla workspace number $ws9
bindsym $mod+agrave workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+eacute move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+egrave move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+ccedilla move container to workspace number $ws9
bindsym $mod+Shift+agrave move container to workspace number $ws10

# resize window (you can also use the mouse for that)
mode "resize" {
        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

# Redémarre polybar quand on redémarre i3
exec_always --no-startup-id "killall polybar;polybar -r left;polybar -r right"
# for window [class=".*"] border pixel 1
